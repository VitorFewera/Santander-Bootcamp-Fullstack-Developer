//Reduce
    //Executa uma função em todos os elementos do array, retornando um valor unico - NÃO CRIA UM NOVO ARRAY

    //Sintaxe
    const callbackFn = function (accumulator, currentValue, index, array){
        //do something
    }

    array.reduce(callbackFn, initialValue);
    //Tem uma função de callback, a função que será executada, a partir do acumulador
    // initialValue => valor sobre o qual o retono final irá atuar(opcional)
    //crrentValue: elemento atual sendo acessado pela função

    



